<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/gato"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/gato"
        android:supportsRtl="true"
        android:theme="@style/Theme.Examen01Forever3"
        tools:targetApi="31">

        <!-- Cambia el orden de las actividades aquí -->
        <activity
            android:name=".LanguageActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="true" />
        <activity
            android:name=".finalActivity"
            android:exported="false" />
        <activity
            android:name=".jugarActivity"
            android:exported="false"
            android:screenOrientation="landscape" /> <!-- Forzar orientación horizontal -->
        <activity
            android:name=".LandActivity"
            android:exported="false" />

    </application>

</manifest>
